import * as Board from "../js/board";

import {
    expect
} from "chai";
import {
    describe,
    it
} from "mocha";

describe("Board", () => {
    it("it loads", () => {
        expect(2).to.equal(1);
    });
});
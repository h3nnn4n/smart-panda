import * as Board from "../js/board";

import {
    expect
} from "chai";
import {
    describe,
    it
} from "mocha";

describe("Board", () => {
    describe.skip("store_board", () => {
        it('runs', () => {
            expect(1).to.equal(1);
        });
    });

    describe.skip("load_board", () => {
        it('runs', () => {
            expect(1).to.equal(1);
        });
    });
});